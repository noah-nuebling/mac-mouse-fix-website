{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "_comment": "Some error when adding i18n told me to add type: module. But that didn't remove the error",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "_comment": "v Call this 'upload', bc 'deploy' seems to be reserved in pnpm",
    "upload": "./scripts/deploy.sh",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxtjs/i18n": "8.0.0-rc.4",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@types/node": "^18.18.3",
    "nuxt": "^3.7.4"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.11",
    "@types/markdown-it": "^13.0.3",
    "assert": "^2.1.0",
    "gsap": "^3.12.2",
    "markdown-it": "^13.0.2"
  }
}
